<import-events
    title="{{ $title }}"
    collection="{{ $collection }}"
    start-date="{{ $start_date }}"
    end-date="{{ $end_date }}"
    all-day="{{ $all_day }}"
    recurrence-rule="{{ $recurrence_rule }}" />
